<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="name.gdr.acastus_photon.MainActivity">

    <!-- if in picker mode cmd_cancel become visible -->
    <item android:id="@+id/cmd_cancel" android:title="@android:string/cancel"
        android:icon="@drawable/ic_action_cancel"
        android:orderInCategory="5" app:showAsAction="always" android:visible="false" />
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_share_location"
        android:orderInCategory="100"
        android:title="@string/action_share_location"
        app:showAsAction="never" />

    <item
        android:id="@+id/clear_recents"
        android:orderInCategory="100"
        android:title="@string/clear_recents_setting"
        app:showAsAction="never" />

    <group
        android:id="@+id/donateGroup">
        <item
            android:id="@+id/donate"
            android:orderInCategory="100"
            android:title="@string/donate_button"
            app:showAsAction="never" />
    </group>
</menu>
